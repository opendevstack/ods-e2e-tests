# Provisioning App 
config.provisioning-app.user.name=${PROV_APP_USER}
config.provisioning-app.user.password=${PROV_APP_PASSWORD}
config.provisioning.url=https://prov-app-${OPENSHIFT_PROJECT}.${OPENSHIFT_PUBLIC_HOST}/

# Jira
config.jira.user.name=${JIRA_USER}
config.jira.user.password=${JIRA_PASSWORD}
config.jira.url=http://${JIRA_URL}/

## Jenkins
#config.jenkins.user.name=${JENKINS_USER}
#config.jenkins.user.password=${JENKINS_PASSWORD}
#config.jenkins.url=https://jenkins-${OPENSHIFT_PROJECT}.${OPENSHIFT_PUBLIC_HOST}/

### Openshift
##config.openshift.user.name=${OPENSHIFT_USER}
##config.openshift.user.password=${OPENSHIFT_PASSWORD}
##config.openshift.url=https://${OPENSHIFT_HOST}/
##
### Reports dir
config.reports.dir=build/e2e-geb-reports
