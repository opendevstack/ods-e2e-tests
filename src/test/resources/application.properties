# User
config.user.name=${PROV_APP_USER}
config.user.password=${PROV_APP_PASSWORD}
# Provisioning app url
config.provisioning.url=https://prov-app-${OPENSHIFT_PROJECT}.${OPENSHIFT_PUBLIC_HOST}/
# Sample app url
#=http://demo-app-carts-${OPENSHIFT_PROJECT}.${OPENSHIFT_PUBLIC_HOST}/carts

# Reports dir
config.reports.dir=build/e2e-geb-reports
